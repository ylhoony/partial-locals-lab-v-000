<%= form_tag students_path, method: :get do %>

  <label>Name Search:</label>
  <%= text_field_tag :name %> 

  <%= submit_tag "Search" %>

<% end %>


<%= @students.each do |std| %>
  <%= render partial: 'student', locals: {student: std} %>
<% end %>
